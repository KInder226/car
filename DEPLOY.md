# 🚀 Инструкция по деплою на Render

## Быстрый старт

### 1. Подготовка репозитория GitHub

```bash
# Инициализация git (если еще не сделано)
git init

# Добавление всех файлов
git add .

# Коммит
git commit -m "Prepare for Render deployment"

# Добавление remote (замените на ваш URL)
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git

# Отправка на GitHub
git branch -M main
git push -u origin main
```

### 2. Деплой через Render Dashboard

1. Зайдите на [render.com](https://render.com) и войдите/зарегистрируйтесь
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub репозиторий
4. Заполните настройки:
   - **Name**: `blog-cars`
   - **Environment**: `Python 3`
   - **Region**: выберите ближайший
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым
   - **Runtime**: `Python 3`
   - **Build Command**: `./build.sh`
   - **Start Command**: `gunicorn blog_project.wsgi:application`
   - **Plan**: `Free`

### 3. Переменные окружения

В разделе **"Environment"** добавьте:

| Ключ | Значение | Описание |
|------|----------|----------|
| `SECRET_KEY` | (сгенерировать) | Используйте: `python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'` |
| `DEBUG` | `False` | Отключить режим отладки |
| `ALLOWED_HOSTS` | (ваш домен) | Будет установлен автоматически после первого деплоя (например: `blog-cars.onrender.com`) |
| `PYTHON_VERSION` | `3.11.0` | Версия Python (опционально) |

### 4. Создание базы данных PostgreSQL

1. В панели Render нажмите **"New +"** → **"PostgreSQL"**
2. Настройки:
   - **Name**: `blog-db`
   - **Database**: `blog`
   - **User**: будет создан автоматически
   - **Region**: тот же, что и веб-сервис
   - **Plan**: `Free`
3. После создания базы данных:
   - Скопируйте **"Internal Database URL"** или **"External Database URL"**
   - Добавьте как переменную окружения `DATABASE_URL` в настройках вашего веб-сервиса

### 5. Первый деплой и настройка

После первого деплоя:

1. Откройте ваш веб-сервис на Render
2. Перейдите в раздел **"Shell"**
3. Выполните миграции:
   ```bash
   python manage.py migrate
   ```
4. Создайте суперпользователя:
   ```bash
   python manage.py createsuperuser
   ```
   (введите логин, email и пароль)

### 6. Доступ к админ-панели

После деплоя откройте:
- **Главная страница**: `https://ваш-домен.onrender.com/`
- **Админ-панель**: `https://ваш-домен.onrender.com/admin/`

## Альтернатива: Использование Blueprint (render.yaml)

Если хотите автоматизировать настройку:

1. Убедитесь, что `render.yaml` находится в корне репозитория
2. На Render: **"New +"** → **"Blueprint"**
3. Подключите репозиторий
4. Render автоматически создаст все сервисы согласно конфигурации

## Важные замечания

⚠️ **Безопасность**:
- Никогда не коммитьте `SECRET_KEY` в репозиторий
- Используйте переменные окружения для всех секретных данных
- В production всегда используйте `DEBUG=False`

📸 **Медиа файлы**:
- На бесплатном плане Render файлы в папке `/media/` не сохраняются между деплоями
- Для production рекомендуется использовать внешнее хранилище:
  - AWS S3
  - Cloudinary
  - Google Cloud Storage

🔧 **Отладка**:
- Логи доступны в разделе **"Logs"** вашего сервиса
- Для проверки конфигурации используйте **"Shell"**

## Troubleshooting

### Ошибка: "No module named 'dj_database_url'"
- Убедитесь, что `requirements.txt` включает `dj-database-url`

### Статические файлы не загружаются
- Проверьте, что `build.sh` включает `collectstatic`
- Убедитесь, что WhiteNoise правильно настроен в `settings.py`

### База данных не подключается
- Проверьте переменную окружения `DATABASE_URL`
- Убедитесь, что база данных создана и запущена
- Используйте "Internal Database URL" для лучшей производительности

### 500 Internal Server Error
- Проверьте логи в разделе "Logs"
- Убедитесь, что `ALLOWED_HOSTS` включает ваш домен
- Проверьте, что все миграции выполнены

