# 🚗 Блог про машины на Django

Современный блог о машинах, созданный на Django с красивым дизайном и функциональностью.

## Возможности

- 📝 Создание и публикация постов о машинах
- 🏷️ Категории (седаны, внедорожники, спорткары и т.д.)
- 🔍 Поиск по постам
- 💬 Комментарии к постам
- 📸 Загрузка изображений для постов
- 📊 Счетчик просмотров
- 📱 Адаптивный дизайн

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Выполните миграции базы данных:
```bash
python manage.py makemigrations
python manage.py migrate
```

3. Создайте суперпользователя:
```bash
python manage.py createsuperuser
```

4. Запустите сервер разработки:
```bash
python manage.py runserver
```

5. Откройте в браузере:
- Главная страница: http://127.0.0.1:8000/
- Админ-панель: http://127.0.0.1:8000/admin/

## Структура проекта

```
Blog/
├── blog/              # Основное приложение
│   ├── models.py      # Модели (CarPost, Category, Comment)
│   ├── views.py       # Представления
│   ├── urls.py        # URL маршруты
│   └── admin.py       # Админ-панель
├── blog_project/      # Настройки проекта
│   ├── settings.py    # Конфигурация
│   └── urls.py        # Главные URL
├── templates/         # HTML шаблоны
│   └── blog/
├── media/             # Загруженные файлы (изображения)
└── static/            # Статические файлы (CSS, JS)
```

## Использование

1. Войдите в админ-панель по адресу `/admin/`
2. Создайте категории машин в разделе "Категории"
3. Создайте посты о машинах в разделе "Посты о машинах"
4. Загрузите изображения для постов
5. Публикуйте посты на главной странице

## Технологии

- Django 4.2+
- Python 3.8+
- Pillow (для работы с изображениями)
- WhiteNoise (для статических файлов)
- Gunicorn (WSGI сервер)
- PostgreSQL (для production)

## Деплой на Render

Проект готов к публикации на Render. Следуйте этим шагам:

### 1. Подготовка репозитория

1. Создайте репозиторий на GitHub
2. Загрузите код в репозиторий:
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/ваш-репозиторий.git
git push -u origin main
```

### 2. Настройка на Render

1. Зайдите на [Render.com](https://render.com) и зарегистрируйтесь/войдите
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Заполните настройки:
   - **Name**: blog-cars (или любое другое)
   - **Environment**: Python 3
   - **Build Command**: `./build.sh`
   - **Start Command**: `gunicorn blog_project.wsgi:application`
   - **Plan**: Free

### 3. Переменные окружения

В разделе "Environment" добавьте:

- `SECRET_KEY` - сгенерируйте новый секретный ключ (можно использовать: `python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'`)
- `DEBUG` - установите `False` для production
- `ALLOWED_HOSTS` - укажите ваш домен Render (например: `your-app.onrender.com`)
- `DATABASE_URL` - будет автоматически добавлен при создании базы данных

### 4. Создание базы данных

1. На Render создайте новую PostgreSQL базу данных:
   - "New +" → "PostgreSQL"
   - Выберите "Free" план
   - После создания скопируйте "Internal Database URL" и добавьте как переменную окружения `DATABASE_URL` для вашего веб-сервиса

### 5. Первый запуск

После первого деплоя выполните миграции через Render Shell:

1. В панели вашего веб-сервиса откройте "Shell"
2. Выполните:
```bash
python manage.py migrate
python manage.py createsuperuser
```

### 6. Статические файлы

Статические файлы автоматически собираются при деплое через `build.sh` скрипт.

### Альтернативный способ: Использование render.yaml

Если вы хотите использовать автоматическую настройку через `render.yaml`:

1. Убедитесь, что файл `render.yaml` находится в корне репозитория
2. На Render: "New +" → "Blueprint"
3. Подключите репозиторий
4. Render автоматически создаст все сервисы и базу данных

## Важные замечания

⚠️ **Безопасность**: 
- Никогда не коммитьте `SECRET_KEY` в репозиторий
- Используйте переменные окружения для всех секретных данных
- `DEBUG=False` в production

📸 **Медиа файлы**: 
- На бесплатном плане Render медиа файлы не сохраняются между деплоями
- Для production рекомендуется использовать внешнее хранилище (AWS S3, Cloudinary и т.д.)

## Лицензия

Этот проект создан в образовательных целях.

